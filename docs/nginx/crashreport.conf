server {
    listen 80;
    listen [::]:80;
    server_name crashreport.libreoffice.org;

    location /static {
        alias /srv/www/static;
    }

    location / {
        uwsgi_pass unix:/run/uwsgi/crashreport.sock;
        include /etc/nginx/uwsgi_params;
    }
}
